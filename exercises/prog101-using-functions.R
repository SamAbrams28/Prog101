# Fill in the blanks so the answers are all 2. Use help for unfamiliar functions
# if necessary.
log10(100)
sqrt(4)
ceiling(1.1)

?ceiling
# Find the functions for cosine and sine. What are the cosine and sine of 0?

# What do you think the following functions do? Just go off the name, don't look
# them up.
read.csv()
# My guess: reads a csv file. Input is probably a file name.
boxplot()
# My guess: creates a visual Input is min, Q1, center, Q2, max.
# Alternatively, maybe argument is a data table, not calculated summary stats.
startsWith()
# My guess: no idea

?read.csv
read.csv("sample csv")

?boxplot

?startsWith
